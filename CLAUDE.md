# CLAUDE.md — ワールドカップ2026（大航海ゲーム集）

## このプロジェクトは何か

ブラウザで遊べる大航海テーマのミニゲーム集。`daiko.html` が本体で、GitHub Pages で公開している。

もとは2026 FIFAワールドカップに合わせた情報発信企画（X→note集客）だったが、**2026-08-19に方針転換**。大会終了で集客企画は畳み、時期に依存しないゲーム部分だけを資産として残すことにした。X投稿文ストック（`posts.md`）と投稿用の企画は削除済み。

したがって**このリポジトリの作業は「集客・収益導線」ではなく「ゲームの作り込み」として扱う**。W杯関連の情報サイト（`ワールドカップサイト.html`）は履歴として残してあるだけで、更新対象ではない。

## 構成

| ファイル | 役割 |
|---|---|
| `index.html` | `ワールドカップサイト.html` へのリダイレクトのみ |
| `daiko.html` | ゲーム集本体（タイトル画面とカード一覧） |
| `daiko.css` | 全ゲーム共通のスタイル |
| `daiko-games.js` | クイズ航海・砲撃戦・宝の地図・大海の覇者 |
| `daiko-piratesim.js` | 海賊船シミュレーター「七つの海の覇者」 |
| `ゲーム開発リサーチ/` | バグ分析レポート・技術ノート・アイデアボード |
| `tests/` | Node上での自動検証 |

海賊シムはローグライト航海バトル。Three.js の海面に2Dオーバーレイを重ねる構成で、海域(leg)を進みながら強化カードを選ぶ。強化16種・敵6種・ボス2種・天候4種。

## 変更したら必ずテストを通す

```
bash tests/run.sh
```

DOM/Canvas をスタブして Node 上でゲーム本体を実走させる。サンドボックスではポート待受ができずブラウザ検証が使えないため、**これが唯一の安全網**。全JSの構文チェックと不正シェルエスケープ検出も含む。難易度バランスの試算は `node tests/balance.js`（2〜3分）。

## 踏んだ地雷（再発しやすい）

- **不正なシェルエスケープ**：`if (\!el)` のバックスラッシュ1文字で `daiko-games.js` 全体が構文エラーになり、全ゲームのカードが無反応になっていた。HTMLコメントも `<\!--` に壊れた。JSを書いたら `node --check` する。`tests/run.sh` の項目2がこれを検出する
- **three.js のCDN**：r150でUMD版 `three.min.js` は廃止済み。ESM版を使う。読めない場合の2D背景フォールバックあり
- **publish.sh はサイト関連ファイルしか add しない**。資料や素材を追加したときは取りこぼすので、別途 `git add` する

## 公開

```
./publish.sh
```

→ `https://tamakisoya1129-coder.github.io/worldcup2026/`

**push すると即座に本番へ反映される。** また、このリポジトリは SSH リモートのため Claude Code のサンドボックスからは push できない（`nc: authentication method negotiation failed`）。**push は必ず本人がターミナルで実行する。**
